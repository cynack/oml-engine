/**
 * @typedef {(OMLDataObject|string)} OMLData
 */

/**
 * @typedef {Object} OMLDataObject
 * @property {string} [version]
 * @property {string} [component]
 * @property {Array.<(number|string)>} [rot]
 * @property {Array.<(number|string)>} [scale]
 * @property {Array.<(number|string)>} [pos]
 * @property {Array.<(number|string)>} [size]
 * @property {Array.<OMLDataObject>} [group]
 */
